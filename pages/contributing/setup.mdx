
## Building from source

Run the following commands to build the project from source:
```bash
git clone https://github.com/frate-dev/frate.git
cd frate
git checkout dev
cmake -DCMAKE_BUILD_TYPE=Debug ./
make -j<jobs>
```


