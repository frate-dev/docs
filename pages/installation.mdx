

## Ubuntu


<a style={{color:"lightblue", fontSize:"1.5em"}} href="/frate.deb" download="frate.deb">Download frate</a>
```bash copy
sudo apt install ~/Downloads/frate.deb
```

## Fedora

## Arch
```bash copy
sudo pacman -Sy --noconfirm base-devel clang cmake libgit2 curl wget git rustup cargo

cargo install --locked sccache
```
## Mac

## Windows
